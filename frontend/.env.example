# Frontend
VITE_API_URL=http://localhost:3001

# Branding (opcional — personalização por cliente na Fase 1)
VITE_APP_NAME=Gestão de Entregas
VITE_APP_SUBTITLE=Gestão de entregas
# Nome curto no ícone instalado (PWA); se vazio, usa abreviação automática
VITE_APP_SHORT_NAME=Entregas

# A chave do Google Maps fica no backend (GOOGLE_MAPS_API_KEY)
# para o cálculo de rotas. O mapa usa OpenStreetMap/Leaflet.

# Web Push (opcional — se vazio, usa /api/notifications/config)
# VITE_VAPID_PUBLIC_KEY=
